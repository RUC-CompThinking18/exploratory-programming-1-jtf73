def double(t):
#defined x as a list
    x = []
    f = []
    d = []
    #Made new list in order to organize other list and return all of them at once
    Azcr = []
    #t = [30, 18, 24, 76]
    for element in t:
        if element in t != int
            print "Numbers only."
            return

        if element in t = int:
            x += (element * 8/2)
            f += (element - 12)
            d += (element + 4)
#Now my code will return one big list with all the changes made to the original list
    Azcr += x
    Azcr += f
    Azcr += d
    return Azcr
