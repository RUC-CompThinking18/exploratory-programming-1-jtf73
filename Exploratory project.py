def double(t):
#defined x as a list
    x = []

    #t = [30, 18, 24, 76]
    for element in t:
        if element in t != int
            print "Numbers only."
            return

        if element in t = int:
            x += (element * 8/2)

    return x
